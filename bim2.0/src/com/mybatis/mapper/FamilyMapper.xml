<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.FamilyMapper">
  <select id="getFamily" parameterType="map" resultType="com.model.Family">
        SELECT * 
        FROM FAMILY_INFO
        WHERE 1=1
        <if test="id != null and id != ''">  
    	    and id = #{id}
        </if>
   </select>
</mapper>